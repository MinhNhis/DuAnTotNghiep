import React from "react";
import { Card, CardContent, Box, Typography, Button } from "@mui/material";
import ProfileAdminTable from "../../components/Profileadmin/ProfileAdminTable";

const ProfileAdmin = () => {
    return (
        <Box>
            <ProfileAdminTable />
        </Box>
    );
};

export default ProfileAdmin;
